--- 核心机器人框架 ---

discord.py==2.3.2          # Discord 官方 API 封装库

--- 异步 Web 服务 (用于 Dashboard API) ---

quart==0.19.4              # 异步版本的 Flask，完美适配 discord.py
quart-cors==0.7.0          # 处理前后端分离的跨域请求 (CORS)

--- 数据库支持 ---

aiosqlite==0.19.0          # 异步 SQLite 库，防止数据库操作阻塞机器人逻辑

--- 环境管理与实用工具 ---

python-dotenv==1.0.0       # 加载 .env 文件中的密钥
aiohttp==3.9.1             # 异步 HTTP 请求处理
psutil==5.9.6              # 用于获取服务器 CPU/内存使用率监控数据

--- 可选：性能增强 ---

uvloop==0.19.0; sys_platform != 'win32'  # Linux 下显著提升异步性能
